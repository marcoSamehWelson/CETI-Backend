@model IEnumerable<Caritas_Egypt_Backend.Models.Service>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>
<div class="card-toolbar" style="gap: 8px;float: right;">
					<form method="post" asp-action="Index" style="display:flex; gap:8px;">
						<input type="text" name="name" value="" />
						<input type="submit" value="Search" class="btn btn-info font-weight-bolder" />
													 </form>


</div>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrainerInsentive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NightTrainerInsentive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ServiceTypeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.languageId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SessionTimeId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrainerInsentive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NightTrainerInsentive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ServiceTypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.languageId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SessionTimeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
